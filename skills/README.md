# Skill map

Core harness has no skill-runtime dependency. This map documents optional workflow upgrades: grilling, domain modeling, codebase design, TDD, Git worktrees, swarm orchestration, and Knip.

Each entry in `sources.lock.json` is pinned to an immutable upstream revision. This repository vendors no third-party skill text in its initial skeleton.
